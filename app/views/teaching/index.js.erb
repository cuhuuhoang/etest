$('#students').html("<%= escape_javascript(render 'students') %>");
$('#paginator').html("<%= escape_javascript(paginate(@students_in_contact, :remote => true)) %>");
$('#students_info').html("<%= escape_javascript(page_entries_info @students_in_contact, :entry_name => 'học sinh') %>");

