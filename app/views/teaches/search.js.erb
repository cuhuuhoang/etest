$('#users').html("<%= escape_javascript(render 'users') %>");
$('#paginator').html("<%= escape_javascript(paginate(@users, :remote => true)) %>");
$('#users_info').html("<%= escape_javascript(page_entries_info @users, :entry_name => opposite_type) %>");

